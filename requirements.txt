# ============================================================
# Requirements – SGMSE+ Speech De-Reverberation
# Samsung Spatial Hackathon – Task 2
# Team: Harsh Singhal (23BEC1021), Sailee Allyadwar (22BCE5211),
#        Navomi S. Ramesh (22BCE1855)
# ============================================================

# ── Core deep learning ───────────────────────────────────────
torch>=1.13.0
torchaudio>=0.13.0

# ── Training framework (SGMSE+ uses PyTorch Lightning 1.x) ──
# Pinned below 2.0 as SGMSE uses the 1.x Trainer API
pytorch-lightning>=1.9.0,<2.0.0

# ── Audio I/O & processing ───────────────────────────────────
soundfile>=0.11.0
librosa>=0.9.2
scipy>=1.9.0
numpy>=1.23.0

# ── Speech quality metrics ───────────────────────────────────
pesq>=0.0.4          # PESQ (wideband)
pystoi>=0.3.3        # ESTOI
fast-bss-eval>=0.1.3 # SI-SDR / SI-SIR / SI-SAR decomposition
torchmetrics>=0.11.0 # General metric utilities

# ── DNSMOS evaluation ────────────────────────────────────────
# Requires the sig_bak_ovr.onnx model from:
# https://github.com/microsoft/DNS-Challenge/tree/master/DNSMOS
onnxruntime>=1.13.0
requests>=2.28.0

# ── Diffusion / score-model utilities ────────────────────────
ml-collections>=0.1.1

# ── Visualisation & logging ──────────────────────────────────
matplotlib>=3.6.0
tensorboard>=2.11.0
tqdm>=4.64.0

# ── General utilities ────────────────────────────────────────
einops>=0.6.0
pandas>=1.5.0
PyYAML>=6.0

# ── Optional: experiment tracking ───────────────────────────
# wandb>=0.13.0
